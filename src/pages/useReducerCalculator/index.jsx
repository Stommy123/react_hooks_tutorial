import React from 'react';
import { Calculator, LayoutContainer } from '../../components';

const useReducerCalculator = _ => (
  <LayoutContainer>
    <Calculator />
  </LayoutContainer>
);

export default useReducerCalculator;
