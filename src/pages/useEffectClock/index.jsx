import React from 'react';
import { Clock, LayoutContainer } from '../../components';

const useEffectClock = _ => (
  <LayoutContainer main>
    <Clock />
  </LayoutContainer>
);

export default useEffectClock;
